 {
    kr_connection 1024;
}

http {
    server {
         80;
        r_name localhost;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}
